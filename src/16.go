package main

import (
    "fmt"
    "math"
)

func main() {
    // Generate a random number between 0 and 9 using math package
    var num int = math.Pi * 2

    // Print the result to console
    fmt.Println(num)
}
